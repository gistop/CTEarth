/**
 * Compatibility entry for code that still imports the historical contents path.
 * New code should import LayerList from the layers feature barrel.
 */
export { LayerList as LayerTree } from '../../features/layers';
